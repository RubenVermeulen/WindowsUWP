<Page
    x:Class="OpendeurdagApp.Views.CampusTourPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpendeurdagApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:carousel="using:CarouselView.Controls"
    xmlns:viewModels="using:OpendeurdagApp.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:CampusTourViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Carousel test" HorizontalAlignment="Center" Margin="0,20"/>
        <carousel:CarouselView x:Name="carousel" ItemWidth="500"  Grid.Row="1" MaxHeight="300" VerticalAlignment="Center">
        </carousel:CarouselView>
        <TextBlock Text="Nice chinese guys" HorizontalAlignment="Center" Margin="0,20" Grid.Row="2"/>
    </Grid>
</Page>
