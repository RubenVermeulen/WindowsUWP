<Page
    x:Class="OpendeurdagClient.View.CampusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpendeurdagClient.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer>
        <StackPanel>
            <!--<TextBlock Text="Overzicht campusen" Style="{ThemeResource HeaderTextBlockStyle}" SelectionChanged="TextBlock_SelectionChanged"></TextBlock>-->
            <TextBlock Text="Overzicht campussen" Style="{ThemeResource HeaderTextBlockStyle}"></TextBlock>


            <ListView ItemsSource="{Binding Campuses}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" 
                                       Style="{ThemeResource SubheaderTextBlockStyle}" />
                            <TextBlock Text="{Binding Address}" 
                                       Style="{ThemeResource CaptionTextBlockStyle}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>

