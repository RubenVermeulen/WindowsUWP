<Page
    x:Class="OpendeurdagClient.View.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpendeurdagClient.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <SplitView IsPaneOpen="True" DisplayMode="Inline" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView.Pane>
            <StackPanel>
                <Button Command="{Binding AllCampusesCommand}"
                        Content="Toon campussen" 
                        Margin="0 0 0 5" />
                <Button Command="{Binding AllDegreesCommand}"
                        Content="Toon opleidingen"
                        
                        />
            </StackPanel>
        </SplitView.Pane>
        
        <SplitView.Content>
            <ContentControl x:Name="mainContent"
                            Content="{Binding CurrentData}"
                            ContentTemplate="{Binding CurrentData.Template}"/>
        </SplitView.Content>
    </SplitView>
</Page>



